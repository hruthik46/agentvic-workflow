# Requirement: REQ-014

# META-LOOP ITERATION 6 (ARCH-IT-ARCH-v11) — INPUT

You are improving the KAIROS multi-agent pipeline (v7.5 LIVE on .106) toward **10/10 production-ready**.

## What v7.5 just shipped (verify before proposing changes — do NOT redo these)

1. **Hermes `tool_use_enforcement: true`** in `/root/.hermes/config.yaml` — root cause of prose-vs-tool-use was `auto` only matching gpt/codex/gemini/gemma/grok; all 8 agents use MiniMax-M2.7 so none matched.
2. **`telegram_alert` retry path** — non-200 detection + Markdown→plain fallback. `TELEGRAM_BOT_TOKEN` env var fix (was reading `TELEGRAM_TOKEN` with truncated fallback → 3-hour silent failure).
3. **`handle_arch_review` JSON parsing** — strips subject prefix, extracts fenced ```json``` block, falls back to first `{…}` regex.
4. **File-inbox quarantine** — bad JSON moves to `/var/lib/karios/agent-msg/quarantine/` instead of looping every poll.
5. **`_update_active_gap_state()`** wired into ARCH-REVIEWED→Phase3 + PROD-DEPLOYED→completed handlers. state.json now stays in sync.
6. **`recover_from_checkpoints` skip-if-state-completed guard** — root cause of 2.5h backend stuck-on-v9-RECOVER loop.
7. **`_sanitize_gap_id()` helper** — caps at 80 chars + splits on em-dash/colon/iteration; prevents ENAMETOOLONG.
8. **Subject aliases** for `[DEPLOYED-PROD]`, `[DEPLOY-DONE]`, `[PRODUCTION-COMPLETE]`, `[DEPLOYED-STAGING]`, `[STAGING-COMPLETE]`, `[BLIND-E2E-RESULTS]`, `[E2E-COMPLETE]`, `[TEST-RESULTS]`, `[ARCH-COMPLETE]`.
9. **`notify_phase_transition` wired into FAN-IN COMPLETE + ARCH-COMPLETE** + the existing 4 handlers.
10. **`3-coding-sync` → Phase 4 transition** added to COMPLETE handler.
11. **Defensive `int(tokens[1])` and `tokens[0]` bounds-checks** in parse_message — no more crashes on em-dash separators or empty subjects.

Verify each via `git log -p /var/lib/karios/orchestrator/event_dispatcher.py` (the file is in a local git inside that dir) or by reading the file directly.

## Your task — DRIVE v11 TO 10/10 PRODUCTION-READY

### A. Pydantic schema at message boundary (carry-over from v10 item B)
- Create `/var/lib/karios/orchestrator/message_schemas.py` with Pydantic v2 models for EACH subject:
  `ArchCompleteBody`, `ArchReviewedBody`, `CodingCompleteBody`, `E2EResultsBody`, `StagingDeployedBody`, `ProdDeployedBody`, `MonitoringCompleteBody`, `RequirementBody`, `ResearchCompleteBody`.
- Validate in `parse_message()` BEFORE dispatching to handlers. On schema violation: log, quarantine to `/var/lib/karios/agent-msg/schema-violations/`, return validation reason to sender via `send_to_agent(<sender>, "[SCHEMA-VIOLATION]", reason)`.
- Apply incrementally: add validation but log-only for one iteration, then enforce on next iteration.

### B. BG-stub-no-op self-test (carry-over from v10 item C)
- Create a `BG-stub-no-op` requirement file at `/var/lib/karios/coordination/requirements/BG-stub-no-op.md` describing a trivially-implementable backend gap.
- Pipeline should: dispatch → architect produces 5 docs → blind-tester reviews → backend writes a no-op handler returning `{"ok":true}` → frontend wires a button → tester confirms 200 OK → blind-tester E2E rates 8/10 → devops deploys → monitor watches.
- Add `karios-self-test` CLI that runs this and asserts each phase boundary fires within timeout. Exit 0 = pipeline healthy.

### C. code-review-graph rubric gate (carry-over from v10 item D)
- Add `agent-worker` post-Hermes hook: parse Hermes session log for `get_minimal_context(...)` calls.
- If task touches code (backend/frontend/devops profiles) and zero `get_minimal_context` calls observed, write critique `karios-vault critique --agent X --failed "skipped get_minimal_context"`.
- Add to dispatcher's phase advance: refuse `[CODING-COMPLETE]` for backend/frontend if their session-summary metadata says `code_review_graph_calls: 0`.

### D. Gitea push verification gate (carry-over from v10 item E)
- After devops emits `[PROD-DEPLOYED]`, dispatcher runs `git -C /root/karios-source-code/<repo> rev-list --left-right --count origin/<branch>...HEAD` for each repo touched in this gap (read from iteration-tracker manifest).
- If any returns non-`0\t0`: refuse `[PROD-DEPLOYED]` transition, send `[GITEA-PUSH-PENDING]` to devops with the diff count, telegram_alert Sai.

### E. Watchdog kill-on-no-tool-call (belt-and-suspenders to `tool_use_enforcement: true`)
- Modify `agent-worker` to spawn Hermes via PTY (not just subprocess.run); stream stdout; count tokens; if `>4000` tokens emitted with zero observed `tool_use` events, send SIGTERM, write critique, retry once with explicit prompt prepend `"BEGIN by calling karios-vault.search. Do not output prose first."`.

### F. Anthropic `tool_choice: any` passthrough (deepest fix; defer if too invasive)
- Investigate `/root/.hermes/hermes-agent/` source. Find provider adapter for OpenAI-compatible endpoints (MiniMax uses this).
- Add profile field `agent.anthropic.tool_choice: any` (or equivalent for OpenAI-compat). Forward through to API call.
- Caveat: incompatible with extended thinking; gate on profile config.

### G. Update obsidian vault + push to gitea
- After all changes: `karios-vault decision --decision-id DEC-17-v7.6 --title "v7.6 production gates" --context ... --decision ... --consequences ...`
- Push to `gitea.karios.ai/KariosD/<repos>` for any source code changes (per existing v7.4 protocol). Do NOT push agentic-workflow files.

## Constraints (REPEATED from v10)
- **No admin telegram from agents** — only `notify_phase_transition()` and `telegram_alert()` from inside dispatcher fire.
- **Use `get_minimal_context(task=...)` FIRST** for any code-touching task (8.2x token reduction).
- **HARD PRE-SUBMIT GATE still applies** — 5 docs ≥2KB before `[ARCH-COMPLETE]`.
- **Tool-use is now enforced at Hermes level** — your sessions will reject if you go prose-only. Use tools.

## You have full vault visibility + code-review-graph + Pydantic + git
```
karios-vault search "<keywords>"
karios-vault recent --kind rca|critique|decision|fix --limit 10
cd /root/karios-source-code/<repo> && uvx code-review-graph serve
pip install pydantic==2.6  # already available in orchestrator venv
git -C /var/lib/karios/orchestrator log --oneline -20
```

Use them BEFORE writing prose. Tool-use enforcement will reject your output otherwise.

## Success criteria for v11
- All 6 items A-F either implemented OR documented as INFEASIBLE with citations.
- BG-stub-no-op self-test passes end-to-end (all 6 phase boundaries fire, all phase events Telegram).
- v11 itself completes all 6 phases NATURALLY (zero synthesized JSON injections at gates).
- `karios-self-test` exit 0.

Pipeline is at 8.5/10 today. Push to 10/10. No half-measures.


_Received: 2026-04-19T22:45:34Z (trace=trace_v11_dispatch)_
